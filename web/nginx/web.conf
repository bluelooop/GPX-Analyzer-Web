# Frontend Server Block
server {
    listen 80;

    # Handle frontend routes
    location ~ ^(?!/api/) {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
